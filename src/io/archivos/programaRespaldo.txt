Programa MiPrograma Inicio
    Entero x, y;
    Real z;
    x = 5;
    y = 10;
    z = (x + y) * 2;
    Leer(x, y);
    Escribir(z, x + y);
    Si x > y Entonces
        z = x - y;
    Sino
        z = y - x;
Fin